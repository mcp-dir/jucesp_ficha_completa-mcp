# Smithery configuration for JUCESP: Ficha Cadastral Completa
# Schema: https://smithery.ai/docs/build/project-config
# Remote HTTP MCP — server hosted at https://api.mcp.ai/p_jucesp_ficha_completa

name: jucesp_ficha_completa-mcp
displayName: "JUCESP: Ficha Cadastral Completa"
description: >
  Ficha cadastral completa de uma empresa na JUCESP (registro, situação e histórico por CNPJ, NIRE ou nome). Hospedado pela plataforma, sem credenciais, pague por consulta com crédito pré-pago.

version: 0.1.0
license: MIT
homepage: https://github.com/mcp-dir/jucesp_ficha_completa-mcp
repository: https://github.com/mcp-dir/jucesp_ficha_completa-mcp

tags:
  - jucesp_ficha_completa
  - brazil
  - horizontal
  - read-only

# Remote HTTP MCP — sem startCommand local (servidor já hospedado)
startCommand:
  type: http
  url: https://api.mcp.ai/p_jucesp_ficha_completa

auth:
  type: oauth2
  resourceMetadataUrl: https://api.mcp.ai/p_jucesp_ficha_completa/.well-known/oauth-protected-resource
  fallback:
    type: agent-auth
    description: >
      Para clientes sem OAuth 2.1. O agente expõe uma tool authenticate(token).
      Usuário gera o JWT em https://app.mcp.ai/agent-auth e cola no chat.
